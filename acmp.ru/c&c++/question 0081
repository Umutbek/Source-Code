#include"stdio.h"
int main()
{
  int i,n,x,min=30000,max=0;
  scanf("%d",&n);
  for (i=1;i<=n;i++) {scanf("%d",&x);
  if (x>max){max=x;};
  if (x<min){min=x;}}
  printf("%d %d",min,max);
  return 0; 
}
