#include<stdio.h>
int main(){
  int a,sum=0,i;
  scanf("%d",&a);
  int arr[a];
  for(int i=0;i<a;i++){
    scanf("%d",&arr[i]);
  }
  if (a==1)
  printf("%d",arr[i]);
  else{
    for(int i=0;i<a-1;i++){
      arr[i]-=1;
      sum+=arr[i];
    }
    printf("%d",sum+arr[a-1]);
  }
  return 0;
}

